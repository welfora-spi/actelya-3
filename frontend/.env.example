# Public backend origin used by the browser. Do not place secrets in frontend env files.
REACT_APP_BACKEND_URL="http://localhost:8001"

# Optional development health endpoints. Leave disabled unless explicitly needed.
ENABLE_HEALTH_CHECK="false"
