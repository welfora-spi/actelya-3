"""Brain — sottopacchetto gateway.

Blocco A: connector_gateway.py (punto unico per le future azioni esterne,
sempre dry-run). Il gateway provider AI dei contenuti (gateway.py) resta
dov'è (backend/app/brain/gateway.py), NON spostato qui, per non rompere gli
import esistenti — vedi principio generale approvato."""
