"""Brain — sottopacchetto memoria (Blocco C).

session.py: memoria temporanea della sessione, esclusivamente in-memory
(nessuna scrittura su disco, nessun MongoDB, nessuna persistenza tra
riavvii). La memoria "completa"/persistente resta fuori scope in questa
fase (vedi vincoli del Blocco C)."""
